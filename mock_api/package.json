{
  "name": "mock_api",
  "version": "1.0.0",
  "description": "json simple mock API",
  "main": "index.js",
  "scripts": {
    "start": "json-server -p 4000 --watch src/db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
